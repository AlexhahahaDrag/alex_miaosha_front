# 功能清单

## 礼尚往来管理

管理端礼尚往来页面位于 `src/views/finance/gift/`，面向后台运营与财务人员，提供高信息密度、表格化、可筛选、可导出的管理体验。

### 页面顺序

1. 数据概览 - 礼尚往来管理
2. 亲友管理 - 礼尚往来管理
3. 事由管理 - 礼尚往来管理
4. 礼金记录 - 礼尚往来管理
5. 统计报表 - 礼尚往来管理

### 页面能力

- 数据概览：展示本月收礼、本月随礼、待回礼、人情净值与趋势。
- 亲友管理：支持搜索、新增、编辑、详情抽屉、历史送礼、历史收礼、人情净值、参与事件。
- 事由管理：支持新增、编辑、删除、汇总统计、收礼人数、随礼人数、总金额。
- 礼金记录：支持高级筛选、分页、排序、汇总统计、Excel 导出、回礼状态列、待回金额列。
- 统计报表：支持月度统计、年度统计、收礼排行、随礼排行、人情净值分析与趋势图表。

### 权限

- 页面权限：`gift:dashboard`、`gift:person`、`gift:event`、`gift:record`、`gift:return`、`gift:analysis`
- 按钮权限：`gift:view`、`gift:add`、`gift:edit`、`gift:delete`、`gift:export`

前端移动端与管理端使用同一套权限标识。超级管理员沿用现有路由逻辑，不需要额外角色授权。

### API 边界

接口封装位于 `src/views/finance/gift/api/`，页面配置位于 `src/views/finance/gift/config/`。接口调用遵循统一响应解构：

```ts
const { code, data, message } = await api();
```
